"""
Configuration template for sync2Ninja
Copy to config.py and modify your values.

This file is NOT versioned.
"""

# Cache directories (relative or absolute paths)
CACHE_DIR = "./mm-sync-cache"
HUNTRESS_CACHE = f"{CACHE_DIR}/huntress.json"
NINJA_CACHE = f"{CACHE_DIR}/ninja_devices.json"
AXCIENT_CACHE = f"{CACHE_DIR}/axcient.json"

# Cache expiration (seconds)
CACHE_TTL = {
    "huntress": 1800,
    "ninja": 1800,
    "axcient": 1800,
}

# API Endpoints
ENDPOINTS = {
    "huntress": {
        "base": "https://api.huntress.io/api/v1",
        "agents": "/agents",
        "orgs": "/organizations",
    },
    "ninja": {
        "auth": "https://api.ninjarmm.com/oauth/token",
        "devices": "https://api.ninjarmm.com/v2/devices",
        "custom_fields": "https://api.ninjarmm.com/v2/devices/{id}/custom-fields",
    },
    "axcient": {
        "base": "https://axapi.axcient.com/x360recover",
        "devices": "/device",
    }
}

