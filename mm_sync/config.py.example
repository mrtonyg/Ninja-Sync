"""
Configuration Template for sync2Ninja
Version: 2.0.8
Author: Anthony George

Rename this file to config.py and adjust values as needed.
"""

import os

# -----------------------------------------------------------------------------
# BASE DIRECTORIES
# -----------------------------------------------------------------------------

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
CACHE_DIR = os.path.join(BASE_DIR, "cache")

if not os.path.isdir(CACHE_DIR):
    os.makedirs(CACHE_DIR, exist_ok=True)

LOG_PATH = os.path.join(BASE_DIR, "sync.log")


# -----------------------------------------------------------------------------
# HUNTRESS CONFIGURATION
# -----------------------------------------------------------------------------

HUNTRESS_BASE_URL = "https://api.huntress.io/api"

# Cache files (NEW — previously missing)
HUNTRESS_CACHE_PATH_AGENTS = os.path.join(CACHE_DIR, "huntress_agents.json")
HUNTRESS_CACHE_PATH_ORGS   = os.path.join(CACHE_DIR, "huntress_orgs.json")

# TTL for Huntress cached data (seconds)
CACHE_TTL_HUNTRESS = 30 * 60  # 30 minutes


# -----------------------------------------------------------------------------
# NINJAONE CONFIGURATION
# -----------------------------------------------------------------------------

NINJA_BASE_URL = "https://api.ninjarmm.com"

# Cache file
NINJA_CACHE_PATH_DEVICES = os.path.join(CACHE_DIR, "ninja_devices.json")

# TTL for Ninja cached devices
CACHE_TTL_NINJA = 30 * 60  # 30 minutes

# Required OAuth scopes for device & custom field operations
# (Ensure these match your NinjaOne App configuration)
NINJA_OAUTH_SCOPE = "monitoring management control"


# -----------------------------------------------------------------------------
# AXCIENT CONFIGURATION
# -----------------------------------------------------------------------------

AXCIENT_BASE_URL = "https://axapi.axcient.com/x360recover"

# Cache for Axcient device inventory
AXCIENT_CACHE_PATH_DEVICES = os.path.join(CACHE_DIR, "axcient_devices.json")

CACHE_TTL_AXCIENT = 30 * 60  # 30 minutes


# -----------------------------------------------------------------------------
# OTHER CONFIGURATION OPTIONS
# -----------------------------------------------------------------------------

# Allow soft-fail on preflight (recommended)
PREFLIGHT_SOFT_FAIL = True

# Enable verbose debug output
DEBUG_MODE = False
