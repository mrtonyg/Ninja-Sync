# Media Managed – Ninja-Sync
# Version: 2.0.9
# Author: Anthony George

# Copy to config.py and update as needed.

# ---------------------------------------------------------------------------
# Directory layout
# ---------------------------------------------------------------------------
CACHE_DIR = "cache"

HUNTRESS_CACHE_AGENTS = f"{CACHE_DIR}/huntress_agents.json"
HUNTRESS_CACHE_ORGS = f"{CACHE_DIR}/huntress_orgs.json"
AXCIENT_CACHE = f"{CACHE_DIR}/axcient_devices.json"
NINJA_CACHE_DEVICES = f"{CACHE_DIR}/ninja_devices.json"

CACHE_TTL_SECONDS = 1800  # 30 minutes

# ---------------------------------------------------------------------------
# API base URLs
# ---------------------------------------------------------------------------
HUNTRESS_BASE_URL = "https://api.huntress.io/api/v1"
AXCIENT_BASE_URL = "https://axapi.axcient.com/x360recover"
NINJA_BASE_URL = "https://api.ninjarmm.com"

# Custom field names in NinjaOne
CUSTOM_FIELD_HUNTRESS = "huntressStatus"
CUSTOM_FIELD_AXCIENT = "backupStatus"
